apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: loco-manager
    app.kubernetes.io/name: controller
    app.kubernetes.io/managed-by: kustomize
  name: loco-manager-metrics-service
  namespace: system
spec:
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: 8443
  selector:
    control-plane: loco-manager
    app.kubernetes.io/name: controller
